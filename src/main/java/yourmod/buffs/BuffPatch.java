package yourmod.buffs;

import com.bord.dice.modthedice.lib.SpireMethod;
import com.bord.dice.modthedice.lib.SpirePatch;
import com.bord.dice.modthedice.lib.SpirePatch2;
import com.tann.dice.gameplay.effect.Buff;

public class BuffPatch {
    /*public interface IMethods {
        boolean getSkipFirstTick();
    }

    @SpirePatch2(clz = Buff.class, method = SpirePatch.CLASS)
    public static class Methods {
        @SpireMethod(from = IMethods.class)
        static boolean getSkipFirstTick(SpireMethod.Helper<Buff, Boolean> helper, boolean ___skipFirstTick)
        {
            return ___skipFirstTick;
        }
    }*/
}
