{
  "modid": "${ModID}",
  "name": "${project.name}",
  "author_list": [
    "Bord",
    "Sefcear",
    "Leonard Curry"
  ],
  "description": "${project.description}",
  "version": "${project.version}",
  "snd_version": "${SliceAndDice.version}",
  "mtd_version": "${ModTheDice.version}",
  "dependencies": [
	"basemod"
  ]
}